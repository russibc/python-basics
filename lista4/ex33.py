# Dado um array numpy (array), como obter uma saída de array numpy que seja igual ao array
# original multiplicado por um escalar?

import numpy as np

arr = np.array([[1, 2], [3, 4]])
n = 5

print(np.multiply(arr, n))
